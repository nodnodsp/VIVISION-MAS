# 仓库交接说明：继续开发指南

## 1. 仓库信息

- GitHub 仓库：<https://github.com/nodnodsp/VIVISION-MAS>
- 本地工作目录：`D:\Codex work`
- 当前主分支：`master`

这个仓库已经整理成适合直接移交到另一个 ChatGPT 账号继续开发的状态。新的账号只需要基于仓库代码和本说明继续，不依赖当前聊天线程。

## 2. 建议新的 ChatGPT 账号先了解这些内容

1. `README.md`
2. `总体需求.txt`
3. `多角度测色仪上位机系统_V1.0_详细功能清单_页面原型说明_含UI示意图.docx`
4. `多角度测色仪上位机系统_UI重构说明_2026-03-28.docx`
5. `多角度测色仪上位机系统_UI连接弹窗与颜色数据显示区设计说明_2026-03-28.docx`
6. `多角度测色仪上位机系统_设置子菜单与配置文件化说明_2026-03-28.docx`
7. `多角度测色仪上位机系统_通讯日志与报文追踪设计说明_2026-03-27.docx`
8. `多角度测色仪上位机系统_仪器运行时抽象设计说明_2026-03-26.docx`
9. `UI改进需求` 目录中的截图素材

## 3. 代码结构

### 核心项目

1. `MAS.Core`
   核心领域模型。
2. `MAS.Application`
   业务接口和服务。
3. `MAS.Infrastructure`
   SQLite 仓储、报告导出、配置存储、备份恢复、仪器运行时工厂等。
4. `MAS.WinUI`
   WPF 主程序。
5. `MAS.Tools`
   命令行验证入口。

### 关键文件

1. `MAS_V1.sln`
2. `MAS.WinUI/MainWindow.xaml`
3. `MAS.WinUI/MainWindow.xaml.cs`
4. `MAS.WinUI/InstrumentConnectWindow.xaml`
5. `MAS.WinUI/InstrumentConnectWindow.xaml.cs`
6. `MAS.WinUI/Services/InstrumentConnectionDiscoveryService.cs`
7. `MAS.Infrastructure/Reports`
8. `MAS.Infrastructure/Runtime`
9. `MAS.Infrastructure/Repositories`
10. `MAS.Infrastructure/Database/Schema/mas_schema.sql`

## 4. 当前功能完成度

### 已完成

1. 主数据管理：样品、标准样、模板、任务。
2. 测量主线：模拟连接、校准、标准样测量、试样测量、结果入库。
3. 数据追溯：测量记录、角度结果、效果结果、操作日志、原始报文。
4. 报告导出：Markdown、HTML、CSV。
5. 运维能力：设置、备份、恢复最近备份、诊断包导出。
6. UI 重构：顶部壳层、菜单、工具栏、仪器连接弹窗、主数据显示表格。

### 未完成或未最终收口

1. 真实串口协议接入，目前仍以 `Simulated` 和 `SerialStub` 为主。
2. 正式 Excel / PDF 报告。
3. 菜单下大部分子项仍是占位交互。
4. 左侧浏览器与更完整业务数据联动。
5. 安装包与发布前系统性回归测试。

## 5. UI 约束与当前共识

1. 顶部不是把菜单并进标题栏，而是标题栏与菜单分两层，但背景一致、视觉连贯。
2. 工具栏按钮使用无边框样式。
3. 顶级菜单使用 `文件 / 仪器 / 色库管理 / 设置 / 关于`。
4. 主数据显示区当前以表格为核心，列为：
   `名称、角度、光源、观察者、仿真色、时间、L*、a*、b*、C*、h*`
5. 光源、观察者、显示项、容差等采用配置文件方式提供。
6. 容差设置使用最小值和最大值编辑框，无需色块。

## 6. 配置与数据位置

运行后常见数据位于输出目录的 `Data` 子目录，例如：
- `MAS.WinUI/bin/Debug/net8.0-windows/Data`
- `MAS.Tools/bin/Debug/net8.0/Data`

已经使用或生成过的配置包括：
1. `ui_option_catalog.json`
2. `tolerance_settings.json`
3. SQLite 数据库文件 `MASQC.db`

## 7. 推荐接手后的第一步

1. 克隆仓库并构建 `MAS_V1.sln`
2. 运行 `MAS.WinUI`
3. 运行 `MAS.Tools` 验证基础业务链
4. 阅读 `UI改进需求` 中的截图
5. 从真实仪器协议或正式报表两条主线里选一条继续推进

## 8. 已验证的最小可行链路

1. 初始化数据库
2. 模拟仪器连接
3. 白板校准
4. 创建任务
5. 标准样测量
6. 试样测量
7. 导出报告
8. 写入日志和原始报文

## 9. 对新的 ChatGPT 账号可直接使用的提示语

可以直接把下面这段话发给新的账号作为起点：

“请基于 GitHub 仓库 `https://github.com/nodnodsp/VIVISION-MAS` 继续开发多角度测色仪上位机。先阅读仓库根目录的 `README.md`、`仓库交接说明_继续开发指南.md`、`总体需求.txt`，以及 `UI改进需求` 目录中的截图和各类中文设计文档。不要重做已有工作，先构建并运行 `MAS_V1.sln`，确认 `MAS.WinUI` 和 `MAS.Tools` 均可运行，然后在现有 WPF 界面和 SQLite 数据主线上继续推进。”

## 10. 风险提示

1. 某些历史文档和生成脚本较多，但不是所有文件都需要立即修改。
2. UI 参考图与现有实现之间仍有差距，继续改时要尽量保留现有业务事件和仓储链路。
3. 若继续做真实设备接入，应优先扩展运行时工厂和串口适配层，而不是把协议逻辑直接写进窗口代码。
