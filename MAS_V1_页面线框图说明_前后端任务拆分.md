# 多角度测色仪上位机系统 V1.0
## 页面线框图说明 + 前后端开发任务拆分清单

版本：V1.0  
日期：2026-03-26  
适用范围：Windows 上位机（WPF + .NET）

---

## 1. 页面线框图说明（文字线框）

### 1.1 启动首页（Dashboard）
页面目标：展示系统状态与快捷入口。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 顶部栏: 产品名 | 版本 | 当前用户 | 时间 | 设备连接状态       |
+-------------------+------------------------------------------+
| 左侧导航          | 中央区                                   |
| - 首页            | [设备状态卡] [校准状态卡] [最近任务卡]   |
| - 仪器连接        | [快捷按钮: 连接/测量/报告]               |
| - 测量中心        | [最近5条测量记录列表]                    |
| - 结果分析        |                                          |
| - 样品管理        |                                          |
| - 颜色库          |                                          |
| - 容差设置        |                                          |
| - 报告中心        |                                          |
| - 系统设置        |                                          |
+-------------------+------------------------------------------+
| 底部状态栏: DB状态 | 日志级别 | 本地缓存 | 同步状态(预留)      |
+--------------------------------------------------------------+
```

关键交互：
1. 点击“连接设备”跳转到仪器连接页。
2. 点击最近任务可直接打开“结果分析页”。
3. 设备未连接时，顶部状态红色高亮并禁用测量快捷按钮。

---

### 1.2 仪器连接页（Instrument Connection）
页面目标：完成设备发现、连接、断开、状态监控、校准入口。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 顶部: 扫描端口 [按钮]  自动连接[开关]  刷新间隔[下拉]          |
+----------------------------+---------------------------------+
| 设备列表区                 | 设备详情区                      |
| [表格]                     | 型号: xxx                       |
| - 端口号                   | SN: xxx                         |
| - 设备名                   | 固件: xxx                       |
| - 驱动状态                 | 连接状态: 已连接/未连接         |
| - 当前占用                 | 最近校准时间: yyyy-mm-dd        |
| - 操作(连接/断开)          | [连接] [断开] [校准]            |
|                            | [查看通信日志]                  |
+----------------------------+---------------------------------+
| 底部通信日志: 时间 | 级别 | 模块 | 消息                        |
+--------------------------------------------------------------+
```

关键交互：
1. 扫描到端口后自动识别匹配设备型号。
2. 连接失败弹出原因（占用、超时、无响应）并建议动作。
3. 点击“校准”打开校准向导弹窗。

---

### 1.3 校准向导弹窗（Calibration Wizard）
页面目标：标准化白板/黑腔校准流程。

线框结构（文本）：
```text
+----------------------------------------+
| 步骤条: 1准备 -> 2白板 -> 3黑腔 -> 完成 |
+----------------------------------------+
| 说明区: 图示 + 操作提示                |
| 实时状态: 等待中/测量中/成功/失败       |
+----------------------------------------+
| [上一步] [下一步] [开始校准] [取消]     |
+----------------------------------------+
```

关键交互：
1. 每步校准都要求设备在线校验。
2. 失败时显示错误码与重试建议。
3. 完成后写入校准日志并刷新连接页状态。

---

### 1.4 测量中心页（Measurement Center）
页面目标：执行标准样与试样测量主流程。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 任务栏: 任务编号 | 操作员 | 容差模板 | 模式(单次/平均/连续)     |
+----------------------+----------------------+----------------------+
| 样品信息区           | 测量操作区           | 实时结果区           |
| 样品编号             | [测量标准样]         | 角度结果表           |
| 样品名称             | [测量试样]           | 角度 | L a b | ΔE    |
| 批次                 | [连续测量开始/停止]   | Sparkle | Graininess |
| 材质                 | 平均次数/间隔设置     | 状态: 合格/预警/失败  |
| 备注                 | 自动命名开关          |                      |
+----------------------+----------------------+----------------------+
| 底部操作: [保存记录] [重测] [清空] [进入结果分析]             |
+--------------------------------------------------------------+
```

关键交互：
1. 未选择标准样时，试样测量弹出“先建标”提醒。
2. 测量完成后自动触发判定引擎并更新结果区。
3. 支持一键保存“任务+结果+角度明细”。

---

### 1.5 结果分析页（Result Analysis）
页面目标：多角度颜色与效果指标的图表分析与判定解释。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 顶部: 样品ID | 标准样ID | 公式切换(76/94/00) | 判定结果             |
+---------------------------+----------------------------------+
| 左侧数据表                | 右侧图表区                       |
| 角度 | L a b | C h | ΔE   | Tab1: 角度对比图                 |
| Sparkle | Graininess      | Tab2: 趋势图                     |
| 通过/警告/失败            | Tab3: 效果指标图                 |
+---------------------------+----------------------------------+
| 底部: 超差说明 | 根因提示(规则命中) | [导出图表] [生成报告]      |
+--------------------------------------------------------------+
```

关键交互：
1. 切换公式后实时重算判定并刷新图表。
2. 点击某角度高亮对应曲线与行数据。
3. 超差项可展开查看“阈值 vs 实测值”。

---

### 1.6 样品管理页（Sample Management）
页面目标：管理样品主数据与历史测量。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 筛选栏: 编号/名称/批次/材质/日期范围 [查询] [重置]            |
+-----------------------------+--------------------------------+
| 样品列表                    | 样品详情                       |
| 编号 | 名称 | 批次 | 状态    | 基础信息                       |
| 最近测量时间 | 最近结论      | 历史测量记录子表               |
|                             | [重新测量] [导出历史]           |
+-----------------------------+--------------------------------+
```

关键交互：
1. 作废仅改状态，不硬删除。
2. 支持从历史记录直接跳转结果分析页。

---

### 1.7 颜色库页（Color Library）
页面目标：管理标准样版本、颜色库导入导出。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 左: 颜色库树(库/分类) | 中: 标准样列表 | 右: 版本详情/比对      |
| [新增库][导入][导出]  | 编号/名称/版本 | 角度值差异、启用状态    |
+--------------------------------------------------------------+
```

关键交互：
1. 标准样支持复制生成新版本。
2. 仅允许一个“默认标准版本”处于启用状态。

---

### 1.8 容差设置页（Tolerance Rules）
页面目标：配置按角度与综合判定阈值。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 左: 模板列表              | 右: 模板详情                     |
| 模板名/适用产品/是否默认   | 色差公式、角度阈值表、综合阈值    |
| [新建][复制][删除][启用]   | [保存] [恢复默认]                |
+--------------------------------------------------------------+
```

关键交互：
1. 模板启用前执行参数合法性校验。
2. 支持“按产品类型”绑定默认模板。

---

### 1.9 报告中心页（Report Center）
页面目标：报告预览、导出、归档查询。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 左: 记录筛选/列表          | 中: 报告预览                     |
| 报告号、样品号、时间、结论  | 页眉、样品信息、角度表、图表、结论 |
|                            | 右: 模板参数与导出按钮           |
|                            | [导出PDF] [导出Excel] [打印]     |
+--------------------------------------------------------------+
```

关键交互：
1. 切换模板后实时预览刷新。
2. 导出后写入“报告导出历史”。

---

### 1.10 系统设置页（System Settings）
页面目标：统一管理基础配置、日志、备份恢复。

线框结构（文本）：
```text
+--------------------------------------------------------------+
| 左侧分类: 常规/测量/数据/日志/关于                            |
+--------------------------------------------------------------+
| 右侧表单:                                                     |
| - 语言/主题                                                   |
| - 默认测量参数                                                 |
| - 数据库路径 + 备份策略                                        |
| - 日志级别 + 日志保留天数                                      |
| [保存设置] [立即备份] [恢复备份]                              |
+--------------------------------------------------------------+
```

关键交互：
1. 关键配置变更提示“重启生效”。
2. 恢复备份前强制二次确认。

---

## 2. 前后端开发任务拆分清单

说明：上位机项目按“UI 层（前端）+ 业务服务/API 层（后端）+ 通信层 + 数据层”拆分。

### 2.1 前端任务清单（WPF/MVVM）

1. 框架与基础组件
- 建立主窗口框架、路由导航、状态栏、全局异常提示组件。
- 建立通用控件库：表格、筛选栏、弹窗、图表容器、状态标签。

2. 页面开发
- `DashboardView`：设备状态卡、快捷入口、最近任务列表。
- `InstrumentConnectionView`：端口列表、设备详情、日志面板、校准入口。
- `CalibrationDialog`：步骤向导与失败重试流程。
- `MeasurementCenterView`：样品表单、测量操作区、实时结果表。
- `ResultAnalysisView`：多角度数据表、公式切换、图表联动。
- `SampleManagementView`：筛选、列表、详情、历史子表。
- `ColorLibraryView`：颜色树、标准样列表、版本比对。
- `ToleranceSettingsView`：模板管理、阈值表单、规则校验展示。
- `ReportCenterView`：预览区、模板参数、导出按钮。
- `SystemSettingsView`：分组设置页、备份恢复入口。

3. 前端业务交互
- 统一命令模式（连接、测量、保存、导出）。
- 输入校验（必填、格式、阈值范围）。
- 数据绑定与异步加载状态（Loading/Empty/Error）。
- 多角度数据高亮规则与颜色标识。

4. 前端非功能
- 启动性能优化（首屏 3 秒内可操作）。
- 崩溃后恢复到最近页面上下文。
- 国际化资源（中文优先，预留英文资源键）。

---

### 2.2 后端任务清单（应用服务/业务引擎）

1. 应用层与服务层
- 建立 `InstrumentService`（连接、断开、状态监测、重连策略）。
- 建立 `CalibrationService`（校准流程、结果记录、有效期校验）。
- 建立 `MeasurementService`（单次/平均/连续测量编排）。
- 建立 `AnalysisService`（色值计算、ΔE计算、效果指标计算）。
- 建立 `RuleEngineService`（容差模板加载、角度判定、综合判定）。
- 建立 `ReportService`（模板渲染、PDF/Excel导出）。

2. 数据层与仓储
- 设计并落地 SQLite 表结构。
- 实现仓储接口：样品、标准样、测量记录、角度结果、模板、报告历史。
- 实现数据库迁移机制（版本升级脚本）。
- 实现备份与恢复服务。

3. 通信层
- 串口枚举与端口占用检测。
- 协议编解码器（命令包、响应包、校验机制）。
- 超时重试、设备忙状态处理、断线重连。
- 驱动检查与异常提示映射。

4. 日志与诊断
- 分级日志（Info/Warn/Error）与模块标签。
- 通信日志、操作日志、异常堆栈日志分文件落盘。
- 诊断包导出（日志 + 环境信息 + 配置快照）。

---

### 2.3 前后端联调任务

1. 仪器连接联调
- 前端连接按钮与后端连接状态事件双向联动。
- 连接异常统一错误码与用户可读提示。

2. 测量流程联调
- 前端触发测量命令后，后端返回实时状态与结果流。
- 标准样/试样切换状态在 UI 上正确同步。

3. 分析与判定联调
- 公式切换触发后端重算并回推前端刷新图表。
- 阈值变更后重新判定并记录“判定版本”。

4. 报告导出联调
- 前端参数选择 -> 后端生成报告 -> 前端回显导出结果。
- 导出失败回传原因与建议。

---

### 2.4 测试与验收任务

1. 功能测试
- 覆盖主流程：连接、校准、测量、判定、报告导出。
- 覆盖异常流程：端口占用、设备离线、测量中断、导出失败。

2. 性能与稳定性测试
- 连续测量压力测试（>=500 次）。
- 长连接稳定性测试（>=8 小时）。

3. 数据一致性测试
- 实时数据、保存数据、导出报告三者一致性校验。
- 备份恢复前后数据完整性校验。

4. 现场验收
- 使用真实仪器和真实样品做试运行验收。
- 输出《问题清单》《修复清单》《发布说明》。

---

## 3. 研发排期建议（10周）

1. 第1-2周：基础框架 + 仪器连接页 + 通信层底座。  
2. 第3-4周：测量中心页 + MeasurementService + 数据入库。  
3. 第5-6周：结果分析页 + RuleEngine + 图表联动。  
4. 第7周：样品管理/颜色库/容差设置页。  
5. 第8周：报告中心 + 导出服务 + 设置页。  
6. 第9周：联调与缺陷修复。  
7. 第10周：现场试运行与发布。

---

## 4. 任务分配建议（角色维度）

1. UI工程师：页面与交互、状态管理、图表联动。  
2. 应用工程师：业务服务、规则引擎、导出服务。  
3. 通信工程师：串口协议、重连策略、驱动兼容。  
4. 测试工程师：自动化回归、压力测试、现场验收。  
5. 项目负责人：需求管控、版本计划、风险闭环。

---

## 5. 交付物清单

1. 《页面线框图说明（文字版）》  
2. 《前后端开发任务拆分清单》  
3. 《联调与测试清单》  
4. 《10周研发排期计划》  
5. 《里程碑验收标准》
