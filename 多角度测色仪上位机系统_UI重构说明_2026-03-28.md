# 多角度测色仪上位机系统 UI 重构说明

## 一、本轮目标
本轮围绕参考软件界面截图，对主窗口进行完整的第一阶段重构，重点目标如下：
1. 将主界面壳层调整为传统桌面工业软件布局。
2. 将标题栏颜色调整为淡紫色，弱化原先绿色风格。
3. 按参考截图补齐菜单栏与传统图标工具区。
4. 将右侧主业务区收敛为样品数据显示表格，减少功能堆叠。
5. 在不破坏既有主流程代码的前提下，对窗口代码进行整理与兼容性修复。
6. 为主样品表补充最小可用的筛选与状态反馈能力。

## 二、本轮界面调整结果
### 1. 主窗口结构
当前主窗口由以下区域组成：
1. 顶部淡紫色标题栏。
2. 传统菜单栏。
3. 图标工具区。
4. 左侧项目浏览器。
5. 右侧样品数据显示主表。
6. 底部状态栏。

### 2. 菜单栏结构
本轮已按参考截图整理如下菜单：
1. 文件(F)
内容包括：新建项目、打开项目、保存项目、另存为、打印、导出、退出。
2. 仪器(I)
内容包括：连接、连接方式、仪器信息、校准、测量标准样、测量试样、测量模式、仪器数据管理、断开连接。
3. 色库管理
内容包括：管理色库、颜色录入。
4. 设置
内容包括：默认光源/观察者、光源设置、默认容差、容差设置、默认显示数据项、显示的数据项、自动命名设置、语言、主题。
5. 帮助(H)
内容包括：帮助、关于。

说明：部分菜单目前为占位交互，已接入统一日志提示，后续逐步补齐业务实现。

### 3. 工具区结构
工具区已按传统桌面软件习惯改为分组图标按钮，当前分组如下：
1. 项目组：新建项目、打开项目、保存项目。
2. 测量组：校准、测量标准、测量试样。
3. 工作区组：测量中心、结果分析、色库管理、样品管理、容差设置。
4. 输出组：打印、导出。

### 4. 左侧项目浏览器
左侧区域已从简单树形控件改为更贴近参考图的项目浏览结构，当前包含：
1. Project001 项目头部。
2. 粗糙面条目。
3. 光滑面条目。
4. 时间信息展示。

### 5. 右侧主视图区
右侧主视图区已收敛为单一表格视图，仅保留样品数据显示能力，当前包含：
1. 标题与简要说明。
2. 样品数量徽标。
3. 筛选工具条。
4. 当前选中样品摘要条。
5. 主样品数据表。

样品表当前支持显示和筛选以下内容：
1. 样品编号。
2. 样品名称。
3. 批次。
4. 材质。
5. 颜色名称。
6. 状态。
7. 更新时间。
8. 关键字筛选。
9. 状态筛选。

### 6. 底部状态栏
底部状态栏已改为动态联动，当前可反映：
1. 是否处于筛选状态。
2. 当前关键字与状态筛选条件。
3. 当前选中的样品编号。

## 三、本轮代码重构点
1. 为主窗口新增统一样式资源。
内容包括：菜单项样式、工具按钮样式、主表头样式、表格单元格样式、表格行选中样式、筛选按钮样式。
2. 修复菜单项与工具按钮共用导航事件时的兼容问题。
说明：导航事件已改为兼容 FrameworkElement，不再只支持 Button。
3. 新增菜单占位交互入口。
说明：暂未落地的菜单点击后会写入运行日志，避免“可见但无响应”。
4. 新增主样品表与隐藏业务页的数据联动。
说明：主视图样品表选中项会同步到原有样品数据逻辑，便于后续继续保留既有业务处理代码。
5. 新增主视图摘要更新方法。
说明：会实时显示样品数量与当前选中样品信息。
6. 新增主样品表筛选逻辑。
说明：支持关键字过滤和状态过滤，并自动刷新主表与状态栏。

## 四、本轮验证结果
1. MAS.WinUI 项目编译通过。
2. 整体解决方案编译通过。
3. 编译结果为 0 warning / 0 error。
4. MAS.Tools 数据链路运行通过。
5. 已验证数据库初始化、测量流程、报告导出未受本轮 UI 重构影响。

## 五、当前已知剩余项
1. 菜单项大部分为第一阶段壳层重建，仍有部分子功能尚未真正实现。
2. 左侧项目浏览器当前为静态展示风格，后续可继续与真实项目数据联动。
3. 右侧表格仍属于中保真实现，后续可继续细化列样式、筛选、状态色与交互反馈。
4. 如果需要完全对齐参考软件，后续还可继续补齐菜单图标、悬停样式和弹出层细节。

## 六、相关文件
1. MainWindow.xaml
2. MainWindow.xaml.cs
3. 主界面改版示意图_2026-03-28_第四版.png
