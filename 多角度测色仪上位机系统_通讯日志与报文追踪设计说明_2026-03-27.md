# 多角度测色仪上位机系统 通讯日志与报文追踪设计说明（2026-03-27）

## 设计背景

当前 MAS V1 已具备任务、测量、报告、操作日志等上层业务能力。
为了后续接入真实多角度测色仪协议，需要在业务闭环之外补上通讯层可观察性。

## 本次设计目标

1. 让通讯请求与响应可落库、可追溯。
2. 让模拟模式和串口待接入模式都遵守同一套报文留痕规则。
3. 让界面上能直接查看报文，不必每次都翻数据库。

## 最小可行方案

本次采用以下 MVP 方案：
1. 复用现有 `raw_packets` 表，不新增数据库结构。
2. 新增 `RawPacket` 实体与仓储接口。
3. 在连接、断开、校准、测量动作处写入原始报文。
4. 在报告中心新增通讯报文视图和筛选。

## 代码改动点

### 1. 领域与仓储

新增：
1. `MAS.Core/Entities/RawPacket.cs`
2. `MAS.Application/Abstractions/IRawPacketRepository.cs`
3. `MAS.Infrastructure/Repositories/SqliteRawPacketRepository.cs`

### 2. 应用层服务

新增：
1. `MAS.Application/Services/RawPacketLogHelper.cs`

调整：
1. `SimulatedInstrumentConnectionService`
2. `SimulatedInstrumentMeasurementService`
3. `SerialStubInstrumentConnectionService`
4. `SerialStubInstrumentMeasurementService`
5. `InstrumentMeasurementRequest` 扩展 `TaskId`、`InstrumentId`
6. `MeasurementWorkflowService` 在测量请求时补齐上下文

### 3. WinUI

调整：
1. 报告中心新增报文类型筛选框。
2. 历史日志区域升级为 Tab 视图。
3. 新增通讯报文 DataGrid。

### 4. 工具链

调整：
1. `MAS.Tools/Program.cs` 新增报文仓储读取与计数输出。

## 运行时行为说明

### Simulated 模式

会写入：
1. 连接请求/响应
2. 断开请求/响应
3. 校准请求/响应
4. 测量请求/响应

### SerialStub 模式

会写入：
1. 串口打开请求和错误响应
2. 校准请求和错误响应
3. 测量请求和错误响应

说明：
虽然当前还未接真实串口，但错误响应本身也会进入报文日志，便于联调时快速定位调用路径。

## 已验证结果

1. 解决方案编译通过，0 warning / 0 error。
2. `MAS.Tools` 已确认最近原始报文数量成功增长。
3. 报告中心已接入通讯报文查看与筛选。

## 后续扩展建议

1. 在报文视图中增加十六进制字段预览与复制。
2. 增加按任务编号、仪器编号、方向的组合筛选。
3. 在真实串口接入后补全发送耗时、重试次数、异常码字段。
