# 多角度测色仪上位机系统 仪器运行时抽象设计说明（2026-03-26）

## 设计背景

当前 MAS V1 的测量流程原本直接依赖模拟仪器服务。
为了降低后续真实设备接入时的改造成本，需要把“运行模式选择”和“服务创建”从界面层里抽出来。

## 本次设计目标

1. 让界面层和工具层不再直接写死模拟服务。
2. 为真实串口接入预留明确的运行模式切换位。
3. 在没有真实协议的情况下，也能通过 Stub 模式提前验证异常提示与交互路径。

## 最小可行方案

本次采用以下 MVP 方案：
1. 保留现有 `IInstrumentConnectionService` 与 `IInstrumentMeasurementService` 抽象。
2. 新增 `InstrumentRuntimeFactory` 统一创建运行时服务。
3. 运行模式由系统设置驱动。
4. 当前提供两种模式：`Simulated`、`SerialStub`。

## 模块改动点

### 1. 配置层

文件：`MAS.Infrastructure/Configuration/AppSettings.cs`

新增字段：
1. `InstrumentRuntimeMode`
2. `InstrumentPortName`
3. `InstrumentBaudRate`
4. `InstrumentReadTimeoutMs`

### 2. 应用层

新增文件：
1. `MAS.Application/Services/SerialStubInstrumentConnectionService.cs`
2. `MAS.Application/Services/SerialStubInstrumentMeasurementService.cs`

说明：
1. `SerialStub` 模式不会伪装成真实成功。
2. 连接、校准、测量会明确提示“协议尚未接入”。
3. 断开动作仍允许执行，避免界面进入不可恢复状态。

### 3. 基础设施层

新增文件：
1. `MAS.Infrastructure/Runtime/InstrumentRuntimeFactory.cs`
2. `MAS.Infrastructure/Runtime/InstrumentRuntimeServices.cs`

说明：
1. 工厂根据系统设置创建当前运行时服务。
2. 工厂同时返回运行时描述，便于界面和工具展示。

### 4. WinUI

改动文件：
1. `MAS.WinUI/MainWindow.xaml`
2. `MAS.WinUI/MainWindow.xaml.cs`

说明：
1. 系统设置新增运行模式与串口参数输入。
2. 主窗口通过运行时工厂获取连接服务和测量服务。
3. 仪器连接与校准失败时会弹出明确提示，不再假定只有模拟成功路径。

### 5. 工具验证层

改动文件：`MAS.Tools/Program.cs`

说明：
1. 命令行工具会先读取系统设置。
2. 再通过工厂选择当前运行模式。
3. 输出当前运行模式描述，便于联调验证。

## 兼容性说明

1. 默认配置仍为 `Simulated`，不会影响当前原型主流程。
2. 既有数据库与历史数据无需迁移。
3. 运行时工厂仅改变服务装配方式，不改变核心业务表结构。

## 已验证结果

1. 解决方案编译通过，0 warning / 0 error。
2. 默认 `Simulated` 模式下工具链主流程运行通过。
3. 多格式报告导出能力保持可用。

## 后续扩展建议

1. 在 `SerialStub` 基础上落真实串口实现。
2. 增加波特率、校验位、停止位、命令超时等更完整的通信配置。
3. 增加通讯日志采集和协议收发调试页。
