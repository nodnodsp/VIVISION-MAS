# 多角度测色仪上位机系统 多格式报告导出设计说明（2026-03-26）

## 设计背景

当前 MAS V1 已具备测量记录、角度结果、效果结果、报告导出记录和报告预览能力。
为了让 1.0 更接近实际质检交付，需要在不引入重型外部依赖的前提下，把报告输出从单一 Markdown 扩展为多格式。

## 本次设计目标

1. 支持多种可交付格式。
2. 不破坏现有 SQLite 数据结构与追溯链路。
3. 不增加复杂第三方组件依赖。
4. WPF 工作台与命令行工具都能验证导出结果。

## 最小可行方案

本次采用以下 MVP 方案：
1. 报告服务新增格式参数。
2. 支持 `md`、`html`、`csv` 三种格式。
3. 导出记录继续写入原有 `report_exports` 表，仅扩展 `FileFormat` 字段使用方式。
4. 报告中心新增格式选择框。
5. 报告预览继续走文件读取方案，长度受系统设置控制。

## 模块改动点

### 1. 应用层

文件：`MAS.Application/Services/MeasurementReportService.cs`

改动：
1. `ExportRecordReportAsync` 新增 `fileFormat` 参数。
2. 增加格式标准化逻辑。
3. 新增 Markdown、HTML、CSV 三个构建函数。
4. 导出记录和操作日志写入格式信息。

### 2. WinUI

文件：`MAS.WinUI/MainWindow.xaml`
`MAS.WinUI/MainWindow.xaml.cs`

改动：
1. 报告中心新增导出格式下拉框。
2. 导出动作会读取当前选择的格式。
3. 报告预览长度改为读取系统设置 `ReportPreviewMaxLength`。

### 3. 工具验证层

文件：`MAS.Tools/Program.cs`

改动：
1. 工具链在单次运行中验证 `md`、`html`、`csv` 三种格式导出。
2. 控制台输出三种报告的实际路径。

## 数据影响

本次不新增数据库表，不修改表结构。
影响仅体现在：
1. `report_exports.FileFormat` 会出现 `md`、`html`、`csv`。
2. 报告路径扩展名与导出格式一致。

## 兼容性说明

1. 原有 Markdown 报告导出能力保持兼容。
2. 原有报告预览、打开文件、打开导出目录等功能继续可用。
3. 历史导出记录不会失效。

## 已验证结果

1. 解决方案编译通过，0 warning / 0 error。
2. 命令行工具已成功导出三种格式报告。
3. WPF 报告中心已接入格式选择。

## 后续扩展建议

1. 增加 Excel / PDF 模板化导出。
2. 为 HTML 报告增加企业 Logo、页眉页脚和打印样式。
3. 为 CSV 增加更标准的批量分析字段布局。
