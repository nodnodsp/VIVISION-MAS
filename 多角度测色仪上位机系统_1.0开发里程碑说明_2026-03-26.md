# 多角度测色仪上位机系统 1.0 开发里程碑说明（2026-03-26）

## 文档目的

本文档用于记录 MAS V1 当前阶段的重要开发节点，作为 1.0 版本持续迭代过程中的中间设计方案与实现说明。
本节点重点覆盖首页总览、主数据管理、任务草稿、测量记录、报告中心、系统设置、仪器运行时抽象七条主线的现状。

## 当前版本定位

当前代码基线已进入 1.0 原型可运行阶段。
系统已具备本地 SQLite 建库、主数据录入、任务草稿生成、标准样/试样测量流程模拟、测量记录保存、多格式报告导出、历史日志查看、系统设置、数据维护、仪器运行模式切换等能力。

## 当前架构概览

1. 领域层：`MAS.Core`
包含仪器、校准记录、试样、标准样、容差模板、测量任务、测量记录、角度结果、效果结果、报告导出、操作日志等核心实体。

2. 应用层：`MAS.Application`
包含任务创建服务、模拟仪器连接服务、模拟测量服务、串口待接入 Stub 服务、测量工作流服务、报告导出服务等业务逻辑。

3. 基础设施层：`MAS.Infrastructure`
包含 SQLite 初始化器、连接工厂、默认数据种子、各业务 Repository 的 SQLite 实现、本地 JSON 系统设置存储、数据维护服务，以及仪器运行时工厂。

4. 界面层：`MAS.WinUI`
采用 WPF 工作台形式，当前已落地为单窗口多页签工作台，覆盖仪器连接、样品管理、标准样管理、模板管理、任务草稿、测量记录、报告中心、系统设置。

5. 工具验证层：`MAS.Tools`
用于快速初始化数据库并执行一条完整验证链路：连接仪器、校准、创建任务、执行标准样测量、执行试样测量、导出报告。

## 已完成模块说明

### 1. 仪器连接与校准模块

已完成内容：
1. 默认仪器种子数据初始化。
2. 仪器连接、断开、白板校准、黑腔校准的模拟服务。
3. 校准记录 SQLite 落库。
4. 仪器页可查看仪器列表和校准历史。
5. 已新增串口待接入模式下的异常提示与运行模式说明。

当前状态：
可用于 1.0 阶段的业务联调和 UI 演示，后续需要替换为真实串口/协议服务。

### 2. 主数据管理模块

已完成内容：
1. 试样管理支持新增、列表展示、选中回填、摘要查看、清空表单。
2. 标准样管理支持新增、列表展示、选中回填、摘要查看、清空表单。
3. 模板管理支持新增、列表展示、选中回填、摘要查看、清空表单。
4. 摘要区可展示关联任务数、记录数、绑定模板、最近测量等信息。

当前状态：
主数据页已经从“单纯录入表单”升级为“可维护、可追溯”的管理页。

### 3. 任务草稿模块

已完成内容：
1. 可根据试样、标准样、模板生成任务草稿。
2. 任务列表支持查看仪器、试样、标准样、模板绑定关系。
3. 新增任务概览面板，可展示任务状态、测量模式、平均次数、最近测量。
4. 新增最近活动面板，可展示最近操作日志与最近报告信息。
5. 支持按任务编号与状态筛选。

当前状态：
任务草稿页已具备从“任务建立”到“任务理解”的基础工作台能力。

### 4. 测量记录与结果分析模块

已完成内容：
1. 支持标准样测量与试样测量流程模拟。
2. 支持手工补录测量记录。
3. 支持角度结果与效果结果查看。
4. 支持按任务编号、记录类型筛选记录。
5. 支持任务上下文摘要与测量分析摘要展示。

当前状态：
测量页已具备 1.0 版本最小闭环能力，可支撑演示、流程验证和后续真实设备接入。

### 5. 报告中心与历史日志模块

已完成内容：
1. 支持导出 Markdown、HTML、CSV 三种报告格式。
2. 报告导出记录落库，并保留格式和路径信息。
3. 支持按报告编号、日志模块筛选。
4. 支持打开导出目录、打开选中报告。
5. 支持报告内容预览，预览长度受系统设置控制。
6. 支持查看最近操作日志。

当前状态：
报告中心已具备“导出、查询、预览、追溯”四个基本能力，并支持更接近实际交付的多格式输出。

### 6. 系统设置与数据维护模块

已完成内容：
1. 新增系统设置页签，并接入本地 JSON 配置文件保存。
2. 支持维护默认任务类型、默认测量模式、默认平均次数、默认测量间隔、默认模板编号、报告预览长度。
3. 支持维护仪器运行模式、默认串口端口、波特率、读取超时。
4. 支持从系统设置页执行数据备份、恢复最近备份、诊断包导出、打开数据目录与备份目录。
5. 新建任务草稿会使用系统设置中的默认模式、次数和间隔。
6. 标准样默认模板编号与报告预览截断长度已接入设置生效链路。

当前状态：
系统设置模块已从“规划项”进入“可保存、可读取、可影响现有流程”的阶段，并具备初步的备份、恢复与诊断导出能力。

### 7. 仪器运行时抽象模块

已完成内容：
1. 新增 `InstrumentRuntimeFactory`，按系统设置选择仪器连接与测量服务。
2. 当前支持两种模式：`Simulated`、`SerialStub`。
3. `Simulated` 模式保持当前原型闭环。
4. `SerialStub` 模式用于后续真实串口协议接入前的占位验证，连接、校准、测量会给出明确提示，不会静默失败。
5. WPF 与命令行工具均改为依赖运行时工厂，而不是直接写死模拟服务。

当前状态：
真实设备尚未接入，但主流程已经具备“运行时可切换”的扩展骨架，后续接串口协议时不需要再反向重构界面层和工具层。

## 本节点新增设计点：多格式报告导出与运行时模式切换

### 设计目标

1. 在不引入额外外部依赖的前提下，提高报告导出的正式程度。
2. 提前为真实串口接入预留运行时切换位，降低后续改造成本。
3. 保持当前 SQLite 与报告记录结构稳定，不破坏既有预览、筛选和追溯链路。
4. 让工具链与 WPF 工作台都能验证导出结果与运行模式。

### 本次实现

1. 报告服务 `MeasurementReportService` 扩展为按格式导出。
2. 当前支持格式：`md`、`html`、`csv`。
3. WPF 报告中心新增导出格式选择框。
4. 报告预览使用系统设置中的最大字符数，不再写死固定长度。
5. 系统设置新增仪器运行模式与串口参数配置。
6. 运行时工厂会根据设置创建模拟服务或串口待接入 Stub 服务。
7. `MAS.Tools` 工具链会输出当前运行模式，并验证三种格式的导出路径。

## 数据流说明

当前主线数据流如下：
1. 初始化数据库并写入默认仪器、默认颜色库。
2. 新增试样、标准样、容差模板。
3. 基于主数据生成任务草稿。
4. 根据系统设置选择当前仪器运行模式。
5. 执行标准样测量与试样测量。
6. 写入测量记录、角度结果、效果结果。
7. 导出报告并写入报告记录。
8. 写入操作日志与校准记录。
9. 在 WPF 工作台中统一展示与追溯。

## 本节点验证结果

本节点完成后已执行以下验证：
1. `MAS_V1.sln` 解决方案编译通过，0 warning / 0 error。
2. `MAS.WinUI` 已随解决方案构建通过。
3. `MAS.Tools` 工具链运行通过。
4. 工具验证链路已成功执行：数据库初始化、模拟仪器连接、白板校准、任务创建、标准样测量、试样测量、Markdown/HTML/CSV 报告导出。
5. 工具输出已确认当前运行模式为 `Simulated`，且三种报告文件都成功生成到本地导出目录。

## 当前 Git 节点说明

截至本文档更新时，关键提交包括：
1. 初始化 MAS V1 代码与文档基线
2. 接入测量记录与结果保存主线
3. 接入仪器连接校准与测量工作流
4. 接入报告导出与历史日志模块
5. 完善报告中心筛选预览与历史追溯界面
6. 增强测量记录分析与任务上下文展示
7. 增强主数据管理页的摘要与表单联动
8. 增强首页概览与任务草稿总览面板
9. 增加任务筛选并沉淀阶段里程碑文档
10. 接入系统设置页与本地配置存储
11. 扩展系统设置页的数据备份与诊断导出
12. 接入数据维护服务与最近备份恢复能力
13. 接入多格式报告导出与预览长度设置联动
14. 接入仪器运行时工厂与串口待接入模式

## 下一阶段开发建议

建议继续按以下顺序推进：
1. 接入真实串口通信与厂家协议适配器，替换 `SerialStub` 实现。
2. 补充正式报表能力，例如 Excel / PDF 模板导出。
3. 优化首页导航与页面跳转，使工作台更接近最终产品形态。
4. 补充异常恢复、备份恢复、安装部署等 1.0 交付能力。
5. 做一次 1.0 集成测试与发布前收口。

## 里程碑结论

MAS V1 当前已经从“需求与设计文档阶段”进入“可运行原型阶段”。
界面、数据库、业务流程、报告导出、系统设置、运行时抽象和日志追溯七个方面均已具备 1.0 原型基础，下一阶段应将重点逐步转向真实设备接入、正式报表和交付完善。
