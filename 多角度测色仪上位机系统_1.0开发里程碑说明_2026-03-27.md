# 多角度测色仪上位机系统 1.0 开发里程碑说明（2026-03-27）

## 本节点定位

本节点是 MAS V1 从“可运行原型”继续向“可接真实设备的交付雏形”推进的一次关键收口。
本次重点补强了通讯日志、原始报文追踪和报告中心的排障视图。

## 本节点新增能力

### 1. 通讯报文落库

已完成内容：
1. 新增 `RawPacket` 实体。
2. 新增 `IRawPacketRepository` 与 SQLite 实现。
3. 已把数据库中的 `raw_packets` 表真正接到代码层。

当前效果：
系统现在可以把运行时的连接、断开、校准、测量请求与响应写入数据库，用于后续排障和协议联调。

### 2. 模拟运行时与串口待接入模式统一接入报文日志

已完成内容：
1. 模拟仪器连接服务会写入连接、断开、校准报文。
2. 模拟测量服务会写入测量请求与测量结果摘要报文。
3. 串口待接入模式会写入串口请求与错误响应报文。

当前效果：
即使真实协议尚未接入，系统也已经具备“看得到通讯链路”的能力，而不是只有业务结果没有底层上下文。

### 3. 报告中心新增通讯报文视图

已完成内容：
1. 报告中心左侧日志区升级为“操作日志 + 通讯报文”双视图。
2. 新增报文类型筛选。
3. 可直接查看方向、类型、任务、仪器、报文文本和时间。

当前效果：
报告中心已经从“导出中心”进一步升级为“历史追溯与排障中心”。

### 4. 工具链验证增强

已完成内容：
1. `MAS.Tools` 现在会统计最近原始报文数量。
2. 运行输出会明确显示通讯日志链路已写入。

当前效果：
开发验证不再只看任务、记录、报告数量，也能确认底层报文落库是否正常。

## 当前能力汇总

截至本节点，MAS V1 已具备：
1. 本地 SQLite 建库与种子初始化。
2. 样品、标准样、容差模板主数据管理。
3. 任务草稿生成与筛选。
4. 标准样/试样测量模拟闭环。
5. 角度结果、效果结果、测量记录落库。
6. Markdown / HTML / CSV 多格式报告导出。
7. 操作日志、通讯报文、历史追溯视图。
8. 系统设置、数据备份、恢复最近备份、诊断包导出。
9. 仪器运行时模式切换：`Simulated` / `SerialStub`。

## 本节点验证结果

本节点完成后已执行以下验证：
1. `MAS_V1.sln` 编译通过，0 warning / 0 error。
2. `MAS.Tools` 运行通过。
3. 工具链已确认最近原始报文数量大于 0。
4. 报告中心界面已接入通讯报文 DataGrid 和筛选框。

## 下一步建议

建议继续按以下顺序推进：
1. 在 `SerialStub` 基础上接入真实串口收发实现。
2. 增加通讯日志的十六进制详情与导出能力。
3. 接入更正式的 Excel / PDF 报表。
4. 做安装包、部署与回归测试收口。

## 结论

MAS V1 当前已经不只是“能跑通业务流程”，还开始具备“能看清底层通讯链路”的研发与排障基础。
这对后续真实多角度测色仪协议接入非常关键。
