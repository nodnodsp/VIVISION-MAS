# 多角度测色仪上位机系统 设置子菜单与配置文件化说明

## 变更目标

本次根据 `D:\Codex work\UI改进需求\菜单` 目录下新增截图，补充了“设置”菜单下的 3 类弹窗，并把光源、观察者和显示数据项改为 JSON 配置文件驱动。

## 已完成内容

### 1. 默认光源 / 观察者弹窗

- 菜单 `设置 -> 默认光源/观察者`
- 菜单 `设置 -> 光源设置`
- 以上两个入口统一打开“光源设置”弹窗。
- 弹窗中提供两个下拉框：
- 光源
- 观察者
- 下拉内容来自本地 JSON 配置文件，而不是写死在界面代码中。
- 当前默认配置项：
- 光源：`A、C、D50、D65、F2、F7、F11`
- 观察者：`2°、10°`

### 2. 显示设置弹窗

- 菜单 `设置 -> 默认显示数据项`
- 菜单 `设置 -> 显示的数据项`
- 两个入口统一打开“显示设置”弹窗。
- 弹窗布局参考截图实现：
- 左侧所有数据项
- 中间加入/移出按钮
- 右侧显示的数据项
- 右侧支持上下移动
- 底部支持恢复到默认和接受
- 当前可配置的数据项与主表字段保持一致：
- 名称
- 角度
- 光源
- 观察者
- 仿真色
- 时间
- L*
- a*
- b*
- C*
- h*
- 配置结果会直接联动主数据显示表格列的显示与隐藏。

### 3. 容差设置弹窗

- 菜单 `设置 -> 默认容差`
- 菜单 `设置 -> 容差设置`
- 两个入口统一打开“容差设置”弹窗。
- 按你的要求，容差设置不再采用前景色块提示形式，改为最小值和最大值编辑框。
- 弹窗包含 3 个页签：
- 颜色容差
- 闪烁度容差
- 综合色差
- 每一项都以表格方式编辑：
- 角度 / 项目
- 最小值
- 最大值

### 4. 配置文件化

本次新增两份 JSON 配置：
- `ui_option_catalog.json`
- `tolerance_settings.json`

运行路径：
- `D:\Codex work\MAS.WinUI\bin\Debug\net8.0-windows\Data\ui_option_catalog.json`
- `D:\Codex work\MAS.WinUI\bin\Debug\net8.0-windows\Data\tolerance_settings.json`

其中：
- `ui_option_catalog.json` 负责光源、观察者、显示数据项目录
- `tolerance_settings.json` 负责默认容差范围配置
- 如果后续文件不存在，程序也会在运行时自动生成默认内容

### 5. 主数据显示区联动

主数据显示区已经联动新的设置项：
- 光源列显示默认光源配置值
- 观察者列显示默认观察者配置值
- 显示设置会实时控制主表列可见性

## 代码文件

### 主窗口与菜单联动
- `D:\Codex work\MAS.WinUI\MainWindow.xaml`
- `D:\Codex work\MAS.WinUI\MainWindow.xaml.cs`

### 设置弹窗
- `D:\Codex work\MAS.WinUI\LightSourceObserverWindow.xaml`
- `D:\Codex work\MAS.WinUI\LightSourceObserverWindow.xaml.cs`
- `D:\Codex work\MAS.WinUI\DisplaySettingsWindow.xaml`
- `D:\Codex work\MAS.WinUI\DisplaySettingsWindow.xaml.cs`
- `D:\Codex work\MAS.WinUI\ToleranceSettingsWindow.xaml`
- `D:\Codex work\MAS.WinUI\ToleranceSettingsWindow.xaml.cs`

### 配置文件模型与存储
- `D:\Codex work\MAS.WinUI\Configuration\UiOptionCatalog.cs`
- `D:\Codex work\MAS.WinUI\Configuration\UiOptionCatalogStore.cs`
- `D:\Codex work\MAS.WinUI\Configuration\ToleranceSettingsDocument.cs`
- `D:\Codex work\MAS.WinUI\Configuration\ToleranceSettingsStore.cs`
- `D:\Codex work\MAS.Infrastructure\Configuration\AppSettings.cs`

## 验证结果

- `MAS.WinUI` 编译通过
- `MAS_V1.sln` 编译通过
- `0 warning / 0 error`
- `MAS.Tools` 运行通过
- 原有主流程：建库 -> 模拟连接/校准 -> 任务 -> 测量 -> 报告导出，未被本次改动破坏

## 当前限制

- 显示设置当前优先联动主数据显示区，尚未扩展到所有明细页和报告页
- 容差设置已完成本地配置化，但还未与测量判定引擎做逐角度规则联动
- 蓝牙设备发现仍受当前机器设备枚举权限影响，受限时会显示回退提示项
