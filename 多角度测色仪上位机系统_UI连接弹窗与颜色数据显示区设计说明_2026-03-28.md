# 多角度测色仪上位机系统 UI 连接弹窗与颜色数据显示区设计说明

## 变更背景

本次 UI 调整基于参考截图目录 `D:\Codex work\UI改进需求` 中的两个设计输入完成：
- 菜单-仪器-连接弹出窗截图
- 数据显示区表格截图

目标是在不破坏当前 1.0 业务链路的前提下，让主窗口的连接入口和颜色数据主表更接近参考软件交互方式。

## 本次完成内容

### 1. 菜单-仪器-连接弹窗

- 在主窗口“菜单 -> 仪器 -> 连接”接入独立弹窗。
- 新增连接弹窗窗口文件：
- `MAS.WinUI/InstrumentConnectWindow.xaml`
- `MAS.WinUI/InstrumentConnectWindow.xaml.cs`
- 弹窗包含以下区域：
- 顶部标题区“连接”
- COM 口下拉框
- 搜索按钮
- 蓝牙设备列表区
- 连接按钮与关闭按钮

### 2. COM 口发现逻辑

- 新增 `InstrumentConnectionDiscoveryService`，用于提供 COM 口与蓝牙设备发现能力。
- COM 口列表改为通过 PowerShell 调用 `.NET SerialPort.GetPortNames()` 获取，避免 `Get-CimInstance Win32_SerialPort` 在部分环境下的权限限制。
- 当前 COM 口列表会按端口号排序后显示在下拉框中。

### 3. 蓝牙设备显示逻辑

- 选择 COM 口后，弹窗自动尝试读取相关蓝牙设备信息。
- 搜索按钮可再次手动刷新。
- 蓝牙发现逻辑优先尝试从 `Win32_PnPEntity` 中读取与 COM 口或蓝牙类设备相关的条目。
- 如果当前环境无法访问设备枚举接口，列表会回退为可显示的提示项，而不是留空或抛错。

### 4. 主数据显示区重构

- 主业务区从原来的样品摘要表，调整为更接近参考图的颜色数据主表。
- 顶部增加标签区：
- 颜色
- 效果色
- 光谱图
- Lab Plot
- 增加筛选区：
- 关键字
- 类型（全部 / standard / trial）
- 筛选按钮
- 清空按钮
- 增加当前选中颜色数据摘要区。

### 5. 主表字段

当前颜色数据显示表格字段已按参考要求调整为：
- 名称
- 角度
- 光源
- 观察者
- 仿真色
- 时间
- L*
- a*
- b*
- C*
- h*

其中：
- 仿真色列使用色块展示，由当前记录的 Lab 值近似换算得到。
- 光源当前默认展示 `D65`。
- 观察者当前默认展示 `2°`。
- C*、h* 根据 a*、b* 动态计算生成。

## 代码变更文件

- `D:\Codex work\MAS.WinUI\MainWindow.xaml`
- `D:\Codex work\MAS.WinUI\MainWindow.xaml.cs`
- `D:\Codex work\MAS.WinUI\InstrumentConnectWindow.xaml`
- `D:\Codex work\MAS.WinUI\InstrumentConnectWindow.xaml.cs`
- `D:\Codex work\MAS.WinUI\Models\MainColorDisplayRow.cs`
- `D:\Codex work\MAS.WinUI\Models\InstrumentConnectionModels.cs`
- `D:\Codex work\MAS.WinUI\Services\InstrumentConnectionDiscoveryService.cs`

## 验证结果

- `MAS.WinUI` 项目编译通过。
- `MAS_V1.sln` 解决方案编译通过。
- 编译结果为 `0 warning / 0 error`。
- `MAS.Tools` 工具链运行通过。
- 运行验证主链路：建库 -> 模拟仪器连接/校准 -> 任务 -> 测量 -> 报告导出，未被本次 UI 改动破坏。
- COM 口发现逻辑已在当前机器终端验证可返回 `COM1`。
- 蓝牙设备发现逻辑已验证具备回退输出能力，在当前受限环境下会显示“当前环境无法读取蓝牙设备信息”。

## 当前限制与后续建议

- 蓝牙设备发现当前仍依赖系统设备枚举能力，不同机器上的返回内容会受驱动和权限影响。
- 当前“效果色 / 光谱图 / Lab Plot”标签仅完成界面壳层，尚未展开成独立内容页。
- 如果后续需要进一步贴近参考软件，可继续补：
- 下拉框与列表框视觉细节
- 蓝牙设备条目图标和状态色
- 主表列宽拖动、冻结列、排序与右键菜单
- 标签切换后的真实业务视图
